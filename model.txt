POST:// /diary => {
    userId: string
    diaryId: string
    diaryName: string
    entries: string[]
}

body: {
    userId: string
    diaryId?: string // used when updating
    diaryName?: string // on create and update
    entries?: string[] // on update only
    sharedWith?: string[] // on update only
}
